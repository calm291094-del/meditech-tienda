name: Keep Alive Backend
on:
  schedule:
    - cron: '*/14 12-23,0-2 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: curl -sS -o /dev/null -w "HTTP %{http_code}\n" --max-time 60 https://meditech-bot.onrender.com/api/health
